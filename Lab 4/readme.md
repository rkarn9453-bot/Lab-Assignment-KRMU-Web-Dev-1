# Prompt Quizzer – JavaScript Quiz Game

This project is created for **Web Development I – Lab Assignment 4**.  
It is a simple quiz game made using JavaScript, and it runs inside the **browser console** using `prompt()` and `alert()`.

---

## 🚀 Features

- Questions stored in an array of objects  
- Uses a `for` loop to display each question  
- Takes user input using `prompt()`  
- Normalizes input using `toLowerCase()` and `trim()`  
- Gives immediate feedback (Correct/Wrong)  
- Increases score for correct answers  
- Shows final score at the end  

---

## 📂 Project File

- **quiz.js** – Contains the complete quiz logic

---

## 🎮 How to Run the Quiz

1. Open **Google Chrome**
2. Right-click anywhere → Click **Inspect**
3. Go to the **Console** tab
4. Open the `quiz.js` file in VS Code and copy all the code
5. Paste the code into the browser console
6. Press **Enter**  
7. The quiz will start automatically

### ▶ To run the quiz again:
Type the following in the console and press Enter:

---

## 🧠 Learning Outcomes

- Using arrays and objects in JavaScript  
- Using loops (`for` loop)  
- Conditional statements (`if-else`)  
- Creating and calling functions  
- Taking input with `prompt()`  
- Showing output with `alert()`  
- Cleaning input using `trim()` and `toLowerCase()`  
- Running JavaScript in the browser console  

---

## 📌 Submission Info

This repository contains:
- `quiz.js`
- `README.md`

Submitted for **Web Development I – Lab Assignment 4 (Prompt Quizzer)**.

---

## 👤 Student Details

- **Name:** Aman Karn  
- **Program:** B.Tech CSE (AI & ML)  
- **Section:** C
